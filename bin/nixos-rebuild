#!/usr/bin/env bash
set -euo pipefail

BIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
FRAMEWORK_DIR="$(cd "$BIN_DIR/.." && pwd)"

source "$FRAMEWORK_DIR/internal/env.sh"
source "$BIN_DIR/lib/nixos-rebuild/main.sh"

rebuild::run "$@"
